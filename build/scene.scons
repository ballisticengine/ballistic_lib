from site_scons import *



src = '../src/scene/'

modules = [
    Glob(src + '*.cpp'),

   
]


env = Environment(CPPPATH=CPPATH)


env.Library('scene', modules, LIBS=ALL_LIBS+['core'],  LIBPATH='../bin', CXXFLAGS=COMMON_CXX_FLAGS, )


