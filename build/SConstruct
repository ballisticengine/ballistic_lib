from site_scons import *

variant_helper = VariantHelper('../bin')

"""SConscript('core.scons', variant_dir='../bin', duplicate=0)
SConscript('io.scons', variant_dir='../bin', duplicate=0)
SConscript('rendering.scons', variant_dir='../bin', duplicate=0)
SConscript('test.scons', variant_dir='../bin', duplicate=0)
SConscript('demo.sdlandpipeline.scons', variant_dir='../bin', duplicate=0)
SConscript('demo.sdlsystem.scons', variant_dir='../bin', duplicate=0)
SConscript('demo.renderingpipeline.scons', variant_dir='../bin', duplicate=0)"""

#variant_helper.build_variant('core.scons')
variant_helper.build_all()